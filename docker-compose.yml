services:
  backend:
    build: .
    container_name: adizes-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./templates:/app/templates
    restart: unless-stopped
